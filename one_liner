pdftotext $( mdfind -interpret '"Goldacre" kind:pdf' | sed 's/ /\\ /g' ) - | awk '{ for(i=1; i<=NF; i++) if($i ~/Goldacre/) { for (x=i-5; x<=i+5; x++ ) {if (x>0 && x<NF) {printf "%s ", $x}} {print ""} } }'
